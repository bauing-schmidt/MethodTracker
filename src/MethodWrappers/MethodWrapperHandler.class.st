Class {
	#name : #MethodWrapperHandler,
	#superclass : #Object,
	#category : #MethodWrappers
}

{ #category : #evaluation }
MethodWrapperHandler >> run: aSelector with: anArray in: aReceiver post: aContext return: anObject carry: carry [

	            
]

{ #category : #evaluation }
MethodWrapperHandler >> run: aSelector with: anArray in: aReceiver pre: aContext [

	            
]
